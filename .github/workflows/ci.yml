name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel in-progress runs on PR force-push or merge into main
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  typecheck-and-test:
    name: Typecheck + unit tests + frontend build
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          # Match the packageManager field in package.json
          version: 9.12.0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        # Mirror Vercel's command — non-frozen lockfile, in case pnpm-lock.yaml drifts.
        # If you want strict reproducibility on CI, switch to --frozen-lockfile.
        run: pnpm install --frozen-lockfile=false

      - name: Typecheck — @quetzal/sdk
        run: pnpm -F @quetzal/sdk typecheck

      # CLI typecheck deliberately skipped on CI:
      # cli/src/commands/*.ts statically imports tests/integration/generated/*.ts,
      # which in turn statically imports contracts/*/target/*.json (Noir build
      # artifacts, ~14 MB total, gitignored, regenerated by `nargo compile`).
      # The SDK loads those bindings via dynamic import at runtime, so it
      # typechecks cleanly without artifacts; the CLI's static-import surface
      # cannot. Running `nargo compile` on CI would require the full Aztec
      # docker toolchain (>15min). CLI typecheck still runs locally pre-commit.
      # See: tests/integration/generated/Token.ts (the artifact-importing layer).

      - name: Typecheck — @quetzal/frontend
        run: pnpm -F @quetzal/frontend typecheck

      - name: Unit tests — @quetzal/sdk (node:test)
        run: pnpm -F @quetzal/sdk test

      - name: Unit tests — @quetzal/cli (node:test)
        run: pnpm -F @quetzal/cli test

      - name: Unit tests — @quetzal/aggregator (node:test)
        run: pnpm -F @quetzal/aggregator test

      - name: Unit tests — @quetzal/frontend (vitest)
        run: pnpm -F @quetzal/frontend test

      - name: Contract artifact VKs match the pinned toolchain
        run: pnpm exec tsx scripts/check-artifact-vks.ts

      - name: Docs addresses match quetzal.config.json
        run: pnpm exec tsx scripts/gen-docs-addresses.ts --check

      - name: Build — @quetzal/frontend (Vite + Node polyfills + SDK browser shims)
        run: pnpm -F @quetzal/frontend build

      # Skipped on purpose (would slow PRs from <2min to >15min):
      #   - L1 Foundry tests (needs forge install + Solidity toolchain)
      #   - L2 Noir TXE tests (needs Docker + nargo via aztec-up)
      #   - Integration tests under tests/ (needs deployed contracts on Aztec sandbox)
      #   - Aggregator package (depends on local Postgres + Aztec stack)
      # If you want these gated, add a separate "weekly" workflow that runs on cron.
